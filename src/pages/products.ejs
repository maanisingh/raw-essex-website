<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products - Raw Essex</title>
    <link href="https://fonts.googleapis.com/css2?family=Archivo:wght@400;600;700&family=Questrial:wght@400&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Questrial', Arial, sans-serif;
            line-height: 1.6;
            color: #1a4c24;
            background-color: #f8fdf9;
        }
        .header {
            background: linear-gradient(135deg, #1a4c24, #2d7c47);
            color: white;
            padding: 1rem 0;
            box-shadow: 0 4px 20px rgba(26, 76, 36, 0.3);
        }
        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        .logo {
            display: flex;
            align-items: center;
            font-family: 'Archivo', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
        }
        .logo img {
            height: 50px;
            margin-right: 15px;
            border-radius: 10px;
        }
        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
            font-family: 'Archivo', sans-serif;
        }
        .nav-links a {
            color: white;
            text-decoration: none;
            font-weight: 600;
            padding: 10px 15px;
            border-radius: 25px;
            transition: all 0.3s;
        }
        .nav-links a:hover {
            background: rgba(255,255,255,0.2);
            color: #7dd87f;
        }
        .container { max-width: 1200px; margin: 0 auto; padding: 40px 20px; }
        .page-header {
            text-align: center;
            margin-bottom: 4rem;
            background: linear-gradient(135deg, #f0fdf4, #dcfce7);
            padding: 3rem;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(26, 76, 36, 0.1);
        }
        .page-header h1 {
            color: #1a4c24;
            margin-bottom: 1rem;
            font-family: 'Archivo', sans-serif;
            font-size: 3rem;
            font-weight: 700;
        }
        .page-header p {
            font-size: 1.2rem;
            color: #4b5563;
        }
        .categories {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-bottom: 4rem;
            flex-wrap: wrap;
        }
        .category-btn {
            padding: 1rem 2rem;
            background: white;
            color: #1a4c24;
            text-decoration: none;
            border-radius: 50px;
            transition: all 0.3s;
            font-family: 'Archivo', sans-serif;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(26, 76, 36, 0.1);
            border: 2px solid transparent;
        }
        .category-btn:hover, .category-btn.active {
            background: linear-gradient(135deg, #4ade80, #22c55e);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(34, 197, 94, 0.3);
            border-color: #16a34a;
        }
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 3rem;
            margin-bottom: 4rem;
        }
        .product-card {
            background: white;
            border-radius: 25px;
            box-shadow: 0 10px 30px rgba(26, 76, 36, 0.1);
            overflow: hidden;
            transition: all 0.4s;
            border: 2px solid transparent;
        }
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(26, 76, 36, 0.2);
            border-color: #4ade80;
        }
        .product-image {
            height: 250px;
            background: linear-gradient(45deg, #f0fdf4, #dcfce7);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1a4c24;
            font-size: 1.2rem;
            position: relative;
            overflow: hidden;
        }
        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s;
        }
        .product-card:hover .product-image img {
            transform: scale(1.05);
        }
        .product-info { padding: 2.5rem; }
        .product-category {
            color: #16a34a;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .product-info h3 {
            margin-bottom: 1rem;
            color: #1a4c24;
            font-family: 'Archivo', sans-serif;
            font-weight: 700;
            font-size: 1.3rem;
            line-height: 1.3;
        }
        .product-description {
            color: #4b5563;
            margin-bottom: 1.5rem;
            font-size: 1rem;
            line-height: 1.6;
        }
        .product-price {
            font-size: 1.8rem;
            font-weight: bold;
            color: #16a34a;
            margin: 1rem 0;
            font-family: 'Archivo', sans-serif;
        }
        .product-status {
            padding: 0.5rem 1.2rem;
            border-radius: 25px;
            font-size: 0.85rem;
            font-weight: bold;
            font-family: 'Archivo', sans-serif;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .in_stock {
            background: linear-gradient(135deg, #dcfce7, #bbf7d0);
            color: #166534;
            border: 1px solid #22c55e;
        }
        .out_of_stock {
            background: linear-gradient(135deg, #fef2f2, #fee2e2);
            color: #dc2626;
            border: 1px solid #ef4444;
        }
        .no-products {
            text-align: center;
            padding: 5rem;
            color: #6b7280;
            background: white;
            border-radius: 25px;
            box-shadow: 0 10px 30px rgba(26, 76, 36, 0.1);
        }
        .no-products h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #1a4c24;
        }
        .footer {
            background: linear-gradient(135deg, #1a4c24, #2d7c47);
            color: white;
            text-align: center;
            padding: 4rem 0;
            margin-top: 3rem;
        }
        .product-count {
            text-align: center;
            margin-bottom: 2rem;
            font-size: 1.1rem;
            color: #4b5563;
        }
        .product-count strong {
            color: #16a34a;
        }
        .search-section {
            margin-bottom: 3rem;
        }
        .search-box {
            display: flex;
            justify-content: center;
            gap: 1rem;
            background: white;
            padding: 2rem;
            border-radius: 25px;
            box-shadow: 0 10px 30px rgba(26, 76, 36, 0.1);
        }
        .search-box input {
            flex: 1;
            max-width: 400px;
            padding: 15px 20px;
            border: 2px solid #22c55e;
            border-radius: 50px;
            font-size: 1.1rem;
            outline: none;
        }
        .search-box input:focus {
            box-shadow: 0 0 15px rgba(34, 197, 94, 0.3);
        }
        .search-box button {
            padding: 15px 25px;
            background: linear-gradient(135deg, #4ade80, #22c55e);
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.2rem;
            transition: all 0.3s;
        }
        .search-box button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(34, 197, 94, 0.3);
        }
    </style>
</head>
<body>
    <header class="header">
        <nav class="nav">
            <div class="logo">
                <img src="/images/raw-essex-logo.jpg" alt="Raw Essex Logo">
                Raw Essex
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/products">All Products</a></li>
                <li><a href="/admin">Admin Panel</a></li>
            </ul>
        </nav>
    </header>

    <div class="container">
        <div class="page-header">
            <h1>Our Premium Products</h1>
            <p>Natural, wholesome nutrition for dogs, cats, birds and other beloved pets</p>
        </div>

        <div class="categories">
            <a href="/products" class="category-btn <%= !selectedCategory ? 'active' : '' %>">All Products</a>
            <a href="/products?category=Raw Dog Food" class="category-btn <%= selectedCategory === 'Raw Dog Food' ? 'active' : '' %>">Raw Dog Food</a>
            <a href="/products?category=Organ Meats" class="category-btn <%= selectedCategory === 'Organ Meats' ? 'active' : '' %>">Organ Meats</a>
            <a href="/products?category=Natural Dog Treats" class="category-btn <%= selectedCategory === 'Natural Dog Treats' ? 'active' : '' %>">Natural Dog Treats</a>
            <a href="/products?category=Dog Supplements" class="category-btn <%= selectedCategory === 'Dog Supplements' ? 'active' : '' %>">Dog Supplements</a>
            <a href="/products?category=Cat Food" class="category-btn <%= selectedCategory === 'Cat Food' ? 'active' : '' %>">Cat Food</a>
            <a href="/products?category=Bird Food" class="category-btn <%= selectedCategory === 'Bird Food' ? 'active' : '' %>">Bird Food</a>
            <a href="/products?category=Accessories" class="category-btn <%= selectedCategory === 'Accessories' ? 'active' : '' %>">Accessories</a>
        </div>

        <div class="search-section">
            <div class="search-box">
                <input type="text" id="productSearch" placeholder="Search products..." value="<%= searchQuery %>">
                <button type="button" onclick="searchProducts()">🔍</button>
            </div>
        </div>

        <% if (products.length > 0) { %>
            <div class="product-count">
                Showing <strong><%= products.length %></strong> premium products
                <% if (selectedCategory) { %>
                    in <strong><%= selectedCategory %></strong>
                <% } %>
                <% if (searchQuery) { %>
                    for "<strong><%= searchQuery %></strong>"
                <% } %>
            </div>
            <div class="products-grid">
                <% products.forEach(product => { %>
                    <div class="product-card">
                        <div class="product-image">
                            <% if (product.image && product.image.startsWith('/images/')) { %>
                                <img src="<%= product.image %>" alt="<%= product.name %>" onerror="this.style.display='none'; this.parentNode.innerHTML='🥩 <%= product.name.substring(0, 25) %>...';">
                            <% } else { %>
                                <span>🥩 <%= product.name.substring(0, 25) %>...</span>
                            <% } %>
                        </div>
                        <div class="product-info">
                            <div class="product-category"><%= product.category %></div>
                            <h3><%= product.name %></h3>
                            <p class="product-description"><%= product.description %></p>
                            <div class="product-price">£<%= product.price.toFixed(2) %></div>
                            <span class="product-status <%= product.status %>">
                                <%= product.status.replace('_', ' ') %>
                            </span>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-products">
                <h3>No products found</h3>
                <p>Try selecting a different category or check back later for new products.</p>
            </div>
        <% } %>
    </div>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 Raw Essex. Premium raw pet food and accessories for healthier, happier pets.</p>
        </div>
    </footer>

    <script>
        function searchProducts() {
            const searchValue = document.getElementById('productSearch').value;
            const currentCategory = new URLSearchParams(window.location.search).get('category') || '';

            let url = '/products?';
            if (searchValue) url += 'search=' + encodeURIComponent(searchValue) + '&';
            if (currentCategory) url += 'category=' + encodeURIComponent(currentCategory);

            window.location.href = url;
        }

        // Allow Enter key to trigger search
        document.getElementById('productSearch').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                searchProducts();
            }
        });

        // Add to cart functionality (placeholder for now)
        function addToCart(productId, productName, price) {
            console.log('Adding to cart:', productId, productName, price);
            // Will implement full cart functionality next
            alert('Product added to cart! (Cart functionality coming soon)');
        }
    </script>
</body>
</html>